@using EntitiyLayer.Concrete
@using X.PagedList
@using X.PagedList.Mvc.Core
@*@model List<Category>*@
@model IPagedList<EntitiyLayer.Concrete.Category>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<h1>Kategori Listesi</h1>
<br />

<table class="table table-bordered">
    <tr>
        <th>#</th>
        <th>Kategori Adı</th>
        <th>Pasif Yap</th>
        <th>Aktif Yap</th>
        <th>Düzenle</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <th>@item.CategoryID</th>
            <td>@item.CategoryName</td>
            <td><a href="/Admin/Category/CategoryDelete/@item.CategoryID" id="DeleteBtn" 
                  onclick="return confirm('Silmek istediğinize emin misiniz?');" class="btn btn-info">Pasif Yap</a></td>


            <td><a href="#" class="btn btn-warning">Aktif Yap</a></td>
            <td><a href="#" class="btn btn-success">Düzenle</a></td>
        </tr>
    }
</table>

<a href="#" class="btn btn-primary">Yeni Kategori</a>

<br />
<br />
@Html.PagedListPager((IPagedList)Model, page => Url.Action("Index", new { page }))
@*
    tanımaldığımız aciton ismi. her defasında index'e page değeri ile gidecek
    gitiği her sayfada page leri getirecek

*@